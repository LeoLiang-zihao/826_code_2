seed: 826
output_dir: outputs/dcc_train_full
data:
  manifest_path: /work/zl467/cxr_project/data/manifests/mimic_subset_manifest.csv
  batch_size: 32
  num_workers: 4
  image_size: 224
model:
  name: resnet50
  pretrained: true
  fine_tune_mode: full
  learning_rate: 0.0001
  weight_decay: 0.0001
  loss_type: focal
  focal_gamma: 2.0
  focal_alpha: 0.75
  label_smoothing: 0.05
  backbone_lr_factor: 0.1
trainer:
  accelerator: gpu
  devices: 1
  max_epochs: 25
  precision: 16-mixed
  deterministic: true
  limit_train_batches: 1.0
  limit_val_batches: 1.0
  log_every_n_steps: 10
  early_stopping_patience: 5
runtime:
  experiment_name: dcc_train_full
  create_manifest_if_missing: false
inference:
  tta: true
